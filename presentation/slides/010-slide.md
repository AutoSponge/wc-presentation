##Custom Elements

This list of slides was loaded with this:

```markup
<core-ajax
    auto
    url="{{ url }}"
    handleAs="document"
    on-core-response="{{ handleFilesList }}">
</core-ajax>
```    